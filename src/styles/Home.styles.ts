import { theme } from "./theme";

export const styles = {
  page: theme.layout.container.base,
  main: theme.layout.container.main,
  titleSection: theme.layout.section.title,
  title: theme.headings.h1,
  subtitle: theme.headings.subtitle,
  grid: theme.layout.grid,
};
